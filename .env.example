# Optional notes for operators (compose reads STREAM_URL via docker-compose.prod.yml).
# Set these if you customize outside compose:

# Public IP or domain of the EC2 instance (for your own scripts / docs)
PUBLIC_HOST=ec2-xx-xx-xx-xx.compute.amazonaws.com

# Backend stream path served through Caddy (same-origin)
STREAM_URL=/novnc/
